# 퀘스트 가이드 HUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 게임 퀘스트 알림처럼, 트리거 달성 시 사용자 머리 옆에 부드럽게 따라오는 가이드 HUD를 띄워 다음 행동을 안내한다.

**Architecture:** InteractionModel/InteractionSetup의 검증된 싱글턴 + SceneEvents.Update 구독 패턴을 그대로 따른다. `QuestModel`(@Observable 싱글턴)이 3단계 시퀀스와 이벤트 진행을 담고, `QuestHUDView`(SwiftUI attachment)가 이를 표시하며, `QuestHUDFollower`(자체 ARKitSession + WorldTrackingProvider)가 head 포즈를 조회해 패널을 lazy-follow 시킨다. 진행 이벤트는 기존 씬 전환/키오스크 버튼(전부 Wade 파일)에서 발행한다.

**Tech Stack:** Swift 6, RealityKit(RealityView attachments, SceneEvents.Update), SwiftUI(@Observable, glassBackgroundEffect), ARKit(WorldTrackingProvider.queryDeviceAnchor), simd.

## Global Constraints

- 스펙: `docs/superpowers/specs/2026-07-13-quest-hud-design.md` — 이 계획의 상위 문서.
- 브랜치: `feature/quest-hud` (이미 생성됨, kimhg 기준). 이 브랜치에서만 작업.
- 이윤서(팀원) 파일 수정은 **`Barrier City/ImmersiveView.swift`의 attachments 블록에 questHUD Attachment 추가, 이 한 곳만** — 사전 허락 완료. 그 외 이윤서 파일(AppModel, HandTrackingManager, ControlPanelView, WheelchairMovementSystem, Dialogue/*)은 **읽기 전용**.
- 이 프로젝트에는 **단위 테스트 타깃이 없다**(앱 타깃 `Barrier City` 하나뿐). 따라서 각 태스크 검증은 (a) `xcodebuild build` 성공 + (b) 지정된 시뮬레이터 수동 확인이다. 예외: Task 1의 순수 진행 로직은 `swiftc` 단독 컴파일로 실제 실패→통과 테스트를 돌린다.
- **모든 `xcodebuild build` 뒤 필수 정리**: ① `git restore "Barrier City.xcodeproj/project.pbxproj"` (DEVELOPMENT_TEAM 자동 변경 원복 — 팀원 서명 설정이라 커밋 금지) ② 생성된 `hyeongikim.rcuserdata` 있으면 삭제. 커밋 전 `git status`로 이 두 가지가 남지 않았는지 확인.
- 빌드 명령(고정):
  ```
  xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
  ```
- 새 파일은 `Barrier City/Quest/` 폴더에 둔다. 폴더 자동 동기화(PBXFileSystemSynchronizedRootGroup)라 pbxproj 수정 불필요.
- 문구는 실기기(손으로 바퀴를 밀어 이동) 흐름 기준으로 확정된 값을 **한 글자도 바꾸지 말 것**(스펙 표 그대로).
- 스크래치 디렉터리(임시 파일용): `/private/tmp/claude-504/-Users-hyeongikim-Documents-2-Projects-barrier-city/4639bd84-53b2-41d6-a057-f1d0194f0b33/scratchpad`

---

## 파일 구성

| 파일 | 책임 | 태스크 |
|---|---|---|
| `Barrier City/Quest/QuestProgression.swift` | import 없는 순수 진행 판정 함수(단독 테스트 대상) | 1 |
| `Barrier City/Quest/QuestModel.swift` | @Observable 싱글턴: 이벤트 enum, 단계 struct, 튜닝 상수, 3단계 시퀀스, advance/reset | 2 |
| `Barrier City/Quest/QuestHUDView.swift` | SwiftUI HUD 패널(목표 카드 + 완료 연출) | 3 |
| `Barrier City/Quest/QuestSetup.swift` | 설치 훅: attachment를 씬 루트에 장착 + follower 시작 + 매 프레임 구독 | 4, 5 |
| `Barrier City/Quest/QuestHUDFollower.swift` | WorldTrackingProvider로 head 포즈 조회 → 패널 lazy-follow + 폴백 | 5 |
| `Barrier City/ImmersiveView.swift` (이윤서) | attachments에 questHUD 1개 추가 | 4 |
| `Barrier City/Interaction/InteractionSetup.swift` (Wade) | install 끝에서 QuestSetup.install 호출 | 4 |
| `Barrier City/Interaction/SceneSwitcher.swift` (Wade) | 실내 진입 성공 시 `.enteredIndoor` 발행 | 6 |
| `Barrier City/Interaction/KioskOrderView.swift` (Wade) | "사용하기" 버튼에서 `.kioskFailed` 발행 | 6 |

---

## Task 1: QuestProgression — 순수 진행 판정

**Files:**
- Create: `Barrier City/Quest/QuestProgression.swift`
- Test(임시): `<스크래치>/main.swift` (커밋 안 함)

**Interfaces:**
- Produces: `enum QuestProgression { static func nextIndex(currentIndex: Int, stepCount: Int, eventMatchesCurrent: Bool) -> Int }` — 이벤트가 현재 단계 완료 조건과 일치하고(=`eventMatchesCurrent`) 아직 끝나지 않았으면 `currentIndex + 1`, 아니면 `currentIndex` 그대로. Task 2의 QuestModel이 호출.

- [ ] **Step 1: 실패 테스트 작성**

스크래치 디렉터리에 `main.swift`를 만든다(top-level 코드는 `main.swift`라는 이름이어야 swiftc가 허용):

파일: `/private/tmp/claude-504/-Users-hyeongikim-Documents-2-Projects-barrier-city/4639bd84-53b2-41d6-a057-f1d0194f0b33/scratchpad/main.swift`
```swift
import Foundation

func expect(_ actual: Int, _ expected: Int, _ name: String) {
    if actual == expected { print("PASS \(name)") }
    else { print("FAIL \(name): got \(actual), want \(expected)"); exit(1) }
}

// 일치 → 다음 단계
expect(QuestProgression.nextIndex(currentIndex: 0, stepCount: 3, eventMatchesCurrent: true), 1, "match advances")
// 불일치 → 그대로(중복/순서 꼬임 방어)
expect(QuestProgression.nextIndex(currentIndex: 0, stepCount: 3, eventMatchesCurrent: false), 0, "mismatch stays")
// 마지막 단계에서 일치 → 종료 인덱스(=stepCount)
expect(QuestProgression.nextIndex(currentIndex: 2, stepCount: 3, eventMatchesCurrent: true), 3, "last match completes")
// 이미 종료 → 그대로
expect(QuestProgression.nextIndex(currentIndex: 3, stepCount: 3, eventMatchesCurrent: true), 3, "done stays")
print("ALL PASS")
```

- [ ] **Step 2: 실패 확인**

Run:
```
swiftc "Barrier City/Quest/QuestProgression.swift" "/private/tmp/claude-504/-Users-hyeongikim-Documents-2-Projects-barrier-city/4639bd84-53b2-41d6-a057-f1d0194f0b33/scratchpad/main.swift" -o /tmp/qtest && /tmp/qtest
```
Expected: 컴파일 실패 — `error: build input file ... QuestProgression.swift ... No such file` 또는 (빈 파일 생성 시) `cannot find 'QuestProgression' in scope`.

- [ ] **Step 3: 최소 구현**

파일: `Barrier City/Quest/QuestProgression.swift`
```swift
//
//  QuestProgression.swift
//  Barrier City
//
//  퀘스트 단계 진행 판정(순수 함수, import 없음 → 단독 테스트 가능).
//  QuestModel이 이벤트-단계 매칭 여부를 넘겨 다음 인덱스를 받는다.
//

enum QuestProgression {
    /// 현재 단계에서 이벤트가 완료 조건과 일치하면 다음 인덱스로.
    /// - currentIndex: 현재 단계 인덱스(0-based). stepCount 이상이면 이미 종료.
    /// - stepCount: 전체 단계 수.
    /// - eventMatchesCurrent: 들어온 이벤트가 현재 단계의 완료 이벤트와 같은가.
    /// - returns: 진행 후 인덱스. 불일치·종료 상태면 currentIndex 그대로.
    static func nextIndex(currentIndex: Int, stepCount: Int,
                          eventMatchesCurrent: Bool) -> Int {
        guard currentIndex >= 0, currentIndex < stepCount else { return currentIndex }
        guard eventMatchesCurrent else { return currentIndex }
        return currentIndex + 1
    }
}
```

- [ ] **Step 4: 통과 확인**

Run:
```
swiftc "Barrier City/Quest/QuestProgression.swift" "/private/tmp/claude-504/-Users-hyeongikim-Documents-2-Projects-barrier-city/4639bd84-53b2-41d6-a057-f1d0194f0b33/scratchpad/main.swift" -o /tmp/qtest && /tmp/qtest
```
Expected:
```
PASS match advances
PASS mismatch stays
PASS last match completes
PASS done stays
ALL PASS
```

- [ ] **Step 5: 커밋**

```bash
git add "Barrier City/Quest/QuestProgression.swift"
git commit -m "feat(quest): 단계 진행 판정 순수 함수 + 단독 테스트"
```

---

## Task 2: QuestModel — 상태·시퀀스·이벤트 API

**Files:**
- Create: `Barrier City/Quest/QuestModel.swift`

**Interfaces:**
- Consumes: `QuestProgression.nextIndex(currentIndex:stepCount:eventMatchesCurrent:)` (Task 1).
- Produces:
  - `enum QuestEvent: Equatable { case enteredIndoor, kioskFailed, npcHelpDone }`
  - `struct QuestStep: Identifiable, Equatable { let id: String; let title: String; let detail: String; let completionEvent: QuestEvent }`
  - `enum QuestTuning`(아래 상수들)
  - `@Observable @MainActor final class QuestModel`: `static let shared`, `let steps: [QuestStep]`, `private(set) var currentIndex: Int`, `private(set) var justCompletedStep: QuestStep?`, `var currentStep: QuestStep?`(계산), `func reset()`, `func advance(on: QuestEvent)`. Task 3 뷰가 `currentStep`/`justCompletedStep`을, Task 4/6이 `shared.reset()`/`shared.advance(on:)`을 사용.

- [ ] **Step 1: 구현 작성**

파일: `Barrier City/Quest/QuestModel.swift`
```swift
//
//  QuestModel.swift
//  Barrier City
//
//  퀘스트 가이드 HUD의 상태 단일 진실원. InteractionModel과 같은
//  @Observable @MainActor 싱글턴 패턴. 진행 판정은 QuestProgression에 위임.
//

import RealityKit
import simd
import Observation

/// 퀘스트 완료를 유발하는 이벤트.
enum QuestEvent: Equatable {
    case enteredIndoor   // 실내(카페) 진입 성공
    case kioskFailed     // 키오스크 "사용하기" → 장벽 안내
    case npcHelpDone     // 정의만 — 발행은 NPC 씬 배치 후(이번 스코프 밖)
}

/// 퀘스트 한 단계(목표 + 방법 + 완료 조건). 순수 값 타입.
struct QuestStep: Identifiable, Equatable {
    let id: String            // 예: "quest.reachCafe"
    let title: String         // 목표(행동)
    let detail: String        // 방법(보조 설명)
    let completionEvent: QuestEvent
}

/// lazy-follow HUD 튜닝 상수 단일 진실원(시뮬레이터에서 보며 조정).
enum QuestTuning {
    /// head 앞으로 띄우는 거리(m)
    static let forwardDistance: Float = 1.2
    /// head 기준 좌우 오프셋(m, -면 왼쪽)
    static let lateralOffset: Float = -0.35
    /// head 기준 세로 오프셋(m, -면 눈높이보다 아래)
    static let verticalOffset: Float = -0.15
    /// 데드존 각도(rad). 이 안이면 따라가지 않음(15°).
    static let deadZoneAngle: Float = 15 * .pi / 180
    /// 데드존 거리(m). 이 안이면 따라가지 않음.
    static let deadZoneDistance: Float = 0.2
    /// 지수 스무딩 수렴 속도(초당 배율 계수). 클수록 빨리 붙는다.
    static let smoothingRate: Float = 4.0
    /// 완료 연출 유지 시간(초).
    static let completedHoldSeconds: Double = 1.5
    /// head 포즈를 못 얻을 때 고정 배치 위치(씬 원점 기준).
    static let fallbackPosition = SIMD3<Float>(-0.35, 1.4, -1.2)
}

/// 퀘스트 전역 상태.
@Observable
@MainActor
final class QuestModel {

    static let shared = QuestModel()

    /// 고정 3단계 시퀀스(문구는 실기기 흐름 기준, 스펙 확정값).
    let steps: [QuestStep] = [
        QuestStep(id: "quest.reachCafe",
                  title: "카페 입구로 이동하세요",
                  detail: "양손으로 바퀴를 잡고 앞으로 밀면 휠체어가 움직입니다",
                  completionEvent: .enteredIndoor),
        QuestStep(id: "quest.tryKiosk",
                  title: "키오스크에서 주문을 시도해 보세요",
                  detail: "키오스크에 가까이 다가가면 화면이 나타납니다",
                  completionEvent: .kioskFailed),
        QuestStep(id: "quest.askStaff",
                  title: "직원에게 도움을 요청하세요",
                  detail: "키오스크가 너무 높아 혼자서는 주문할 수 없습니다",
                  completionEvent: .npcHelpDone),
    ]

    /// 현재 단계 인덱스. steps.count면 전체 완료(이번 스코프에선 도달 안 함).
    private(set) var currentIndex = 0
    /// 완료 연출용: 방금 완료된 단계(잠시 표시 후 nil).
    private(set) var justCompletedStep: QuestStep?

    /// 현재 표시할 목표 단계(전체 완료면 nil).
    var currentStep: QuestStep? {
        currentIndex < steps.count ? steps[currentIndex] : nil
    }

    /// 몰입 공간 재진입 시 1단계로 리셋(InteractionModel 리셋 패턴과 동일).
    func reset() {
        currentIndex = 0
        justCompletedStep = nil
    }

    /// 이벤트 발행. 현재 단계의 완료 이벤트와 일치할 때만 진행하고 완료 연출을 띄운다.
    /// 불일치 이벤트(중복·순서 꼬임)는 무시된다.
    func advance(on event: QuestEvent) {
        guard let step = currentStep else { return }
        let matches = (event == step.completionEvent)
        let next = QuestProgression.nextIndex(currentIndex: currentIndex,
                                              stepCount: steps.count,
                                              eventMatchesCurrent: matches)
        guard next != currentIndex else { return }   // 변화 없으면 무시
        justCompletedStep = step
        currentIndex = next
        // 완료 연출을 completedHoldSeconds 후 해제(그 사이 새 완료가 오면 최신 것 우선).
        Task { @MainActor [self] in
            try? await Task.sleep(for: .seconds(QuestTuning.completedHoldSeconds))
            if self.justCompletedStep == step { self.justCompletedStep = nil }
        }
    }
}
```

- [ ] **Step 2: 빌드 확인**

Run:
```
xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 3: 빌드 산출물 정리**

Run:
```
git restore "Barrier City.xcodeproj/project.pbxproj"
find "Barrier City.xcodeproj" -name "*.rcuserdata" -exec rm -rf {} + 2>/dev/null; true
git status --short
```
Expected: `git status --short`에 `Barrier City/Quest/QuestModel.swift`만 나오고 pbxproj/rcuserdata 변경 없음.

- [ ] **Step 4: 커밋**

```bash
git add "Barrier City/Quest/QuestModel.swift"
git commit -m "feat(quest): QuestModel 싱글턴 — 3단계 시퀀스·이벤트 진행·완료 연출 상태"
```

---

## Task 3: QuestHUDView — HUD 패널 UI

**Files:**
- Create: `Barrier City/Quest/QuestHUDView.swift`

**Interfaces:**
- Consumes: `QuestModel.shared.currentStep`, `QuestModel.shared.justCompletedStep`(Task 2).
- Produces: `struct QuestHUDView: View`. Task 4에서 `Attachment(id: "questHUD") { QuestHUDView() }`로 사용.

- [ ] **Step 1: 구현 작성**

파일: `Barrier City/Quest/QuestHUDView.swift`
```swift
//
//  QuestHUDView.swift
//  Barrier City
//
//  퀘스트 가이드 HUD 패널. QuestModel.shared를 관찰해 현재 목표를 보여주고,
//  단계 완료 순간에는 잠깐 "완료!" 연출을 띄운다. 위치·빌보드는 QuestSetup/
//  QuestHUDFollower가 처리하고, 이 뷰는 내용만 담당한다.
//

import SwiftUI

struct QuestHUDView: View {

    var body: some View {
        // @Observable 싱글턴: body에서 읽는 값이 관찰 의존성이 된다.
        let quest = QuestModel.shared

        Group {
            if let done = quest.justCompletedStep {
                completed(done)
            } else if let step = quest.currentStep {
                objective(step)
            } else {
                EmptyView()   // 전체 완료(이번 스코프에선 도달 안 함)
            }
        }
        .animation(.spring(duration: 0.4), value: quest.justCompletedStep)
        .animation(.spring(duration: 0.4), value: quest.currentStep)
    }

    /// 완료 순간 연출.
    private func completed(_ step: QuestStep) -> some View {
        VStack(spacing: 10) {
            Image(systemName: "checkmark.circle.fill")
                .font(.system(size: 40))
                .foregroundStyle(.green)
            Text("완료!").font(.title2).bold()
            Text(step.title).font(.callout).foregroundStyle(.secondary)
        }
        .padding(24)
        .frame(width: 420)
        .glassBackgroundEffect()
        .transition(.scale.combined(with: .opacity))
    }

    /// 상시 목표 카드.
    private func objective(_ step: QuestStep) -> some View {
        VStack(alignment: .leading, spacing: 8) {
            Text("목표")
                .font(.caption).bold()
                .foregroundStyle(.secondary)
                .padding(.horizontal, 10).padding(.vertical, 3)
                .background(.tint.opacity(0.25), in: Capsule())
            Text(step.title).font(.title2).bold()
            Text(step.detail).font(.callout).foregroundStyle(.secondary)
        }
        .frame(width: 420, alignment: .leading)
        .padding(24)
        .glassBackgroundEffect()
        .transition(.scale.combined(with: .opacity))
    }
}

#Preview(windowStyle: .automatic) {
    QuestHUDView()
}
```

- [ ] **Step 2: 빌드 확인**

Run:
```
xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 3: 빌드 산출물 정리**

Run:
```
git restore "Barrier City.xcodeproj/project.pbxproj"
find "Barrier City.xcodeproj" -name "*.rcuserdata" -exec rm -rf {} + 2>/dev/null; true
git status --short
```
Expected: `Barrier City/Quest/QuestHUDView.swift`만 변경으로 표시.

- [ ] **Step 4: 커밋**

```bash
git add "Barrier City/Quest/QuestHUDView.swift"
git commit -m "feat(quest): 퀘스트 HUD 패널 뷰(목표 카드 + 완료 연출)"
```

---

## Task 4: QuestSetup + attachment 장착 (고정 배치 먼저)

이 태스크는 HUD를 화면에 **고정 위치로** 띄우는 것까지 완성한다(follower는 Task 5). 이윤서 파일 1줄 수정이 여기 포함된다.

**Files:**
- Create: `Barrier City/Quest/QuestSetup.swift`
- Modify: `Barrier City/ImmersiveView.swift` (attachments 블록, 이윤서 파일 — 허락 완료)
- Modify: `Barrier City/Interaction/InteractionSetup.swift` (install 끝)

**Interfaces:**
- Consumes: `QuestModel.shared.reset()`(Task 2), `QuestHUDView`(Task 3), `QuestTuning.fallbackPosition`(Task 2).
- Produces: `enum QuestSetup { static func install(content: RealityViewContent, attachments: RealityViewAttachments, appModel: AppModel) }`. Task 5에서 이 안에 follower 로직을 붙인다.

- [ ] **Step 1: QuestSetup 작성(고정 배치 버전)**

파일: `Barrier City/Quest/QuestSetup.swift`
```swift
//
//  QuestSetup.swift
//  Barrier City
//
//  퀘스트 HUD 설치 훅. InteractionSetup.install() 끝에서 한 번 호출된다.
//  HUD attachment를 '씬 루트'(worldRoot가 아니라 content)에 붙여 맵과 분리하고,
//  매 프레임 위치를 갱신한다. (Task 5에서 lazy-follow를 추가; 지금은 고정 배치.)
//

import RealityKit
import SwiftUI

@MainActor
enum QuestSetup {
    private static var hudPanel: Entity?
    private static var subscription: EventSubscription?

    static func install(content: RealityViewContent,
                        attachments: RealityViewAttachments,
                        appModel: AppModel) {
        // 재진입마다 1단계로 리셋(InteractionModel 리셋 패턴과 동일).
        QuestModel.shared.reset()

        guard let panel = attachments.entity(for: "questHUD") else {
            print("⚠️ questHUD attachment 없음 — 퀘스트 HUD 비활성")
            return
        }
        content.add(panel)   // 씬 루트에 직접(HUD는 맵과 함께 움직이면 안 된다)
        hudPanel = panel

        // 일단 원점 앞 고정 배치(Task 5에서 head lazy-follow로 대체).
        panel.setPosition(QuestTuning.fallbackPosition, relativeTo: nil)
        let p = QuestTuning.fallbackPosition
        let yaw = atan2(-p.x, -p.z)   // 원점(사용자)을 향하도록(InteractionSetup 관례)
        panel.setOrientation(simd_quatf(angle: yaw, axis: [0, 1, 0]), relativeTo: nil)

        subscription = content.subscribe(to: SceneEvents.Update.self) { _ in
            // Task 5에서 follower 갱신을 여기에 넣는다. 지금은 고정이라 할 일 없음.
        }
    }
}
```

- [ ] **Step 2: InteractionSetup.install 끝에서 QuestSetup 호출**

파일: `Barrier City/Interaction/InteractionSetup.swift` — 기존 install 함수 끝의 구독 등록 블록 바로 뒤(현재 파일에서 `im.updateSubscription = content.subscribe(...)` 닫는 `}` 다음, 함수 닫는 `}` 앞)에 추가:
```swift
        // 3) 매 프레임 근접 판정 구독(구독 객체를 보관해야 해제되지 않는다).
        im.updateSubscription = content.subscribe(to: SceneEvents.Update.self) { _ in
            tick()
        }

        // 4) [김현기] 퀘스트 가이드 HUD 설치(HUD는 씬 루트에 붙고 head를 따라간다).
        QuestSetup.install(content: content, attachments: attachments, appModel: appModel)
    }
```

- [ ] **Step 3: ImmersiveView attachments에 questHUD 추가 (이윤서 파일)**

파일: `Barrier City/ImmersiveView.swift` — attachments 블록(현재 `kioskScreen` Attachment 뒤, 블록 닫는 `}` 앞)에 추가:
```swift
            // [김현기] 키오스크 주문 화면(고정 높이 장벽은 InteractionSetup이 처리)
            Attachment(id: "kioskScreen") {
                KioskOrderView()
            }
            // [김현기] 퀘스트 가이드 HUD(head lazy-follow는 QuestSetup이 처리)
            Attachment(id: "questHUD") {
                QuestHUDView()
            }
        }
```

- [ ] **Step 4: 빌드 확인**

Run:
```
xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 5: 빌드 산출물 정리**

Run:
```
git restore "Barrier City.xcodeproj/project.pbxproj"
find "Barrier City.xcodeproj" -name "*.rcuserdata" -exec rm -rf {} + 2>/dev/null; true
git status --short
```
Expected: 변경은 `Barrier City/Quest/QuestSetup.swift`, `Barrier City/ImmersiveView.swift`, `Barrier City/Interaction/InteractionSetup.swift` 3개만. pbxproj/rcuserdata 없음.

- [ ] **Step 6: 시뮬레이터 수동 확인**

Xcode에서 visionOS 시뮬레이터로 실행 → 몰입 공간 진입 시 사용자 앞쪽에 "목표 / 카페 입구로 이동하세요 / 양손으로 바퀴를 잡고..." HUD 카드가 유리 배경으로 뜬다. (아직 head를 따라가지 않고 원점 앞 고정이어도 정상 — Task 5에서 follow 추가.)

- [ ] **Step 7: 커밋**

```bash
git add "Barrier City/Quest/QuestSetup.swift" "Barrier City/ImmersiveView.swift" "Barrier City/Interaction/InteractionSetup.swift"
git commit -m "feat(quest): HUD attachment 씬 루트 장착 + 설치 훅 연결(고정 배치)"
```

---

## Task 5: QuestHUDFollower — head lazy-follow

**Files:**
- Create: `Barrier City/Quest/QuestHUDFollower.swift`
- Modify: `Barrier City/Quest/QuestSetup.swift` (follower 보관·시작·매 프레임 갱신)

**Interfaces:**
- Consumes: `QuestTuning`(Task 2).
- Produces: `@MainActor final class QuestHUDFollower { func start() async; func update(panel: Entity, dt: Float); func stop() }`. QuestSetup이 소유한다.

- [ ] **Step 1: QuestHUDFollower 작성**

파일: `Barrier City/Quest/QuestHUDFollower.swift`
```swift
//
//  QuestHUDFollower.swift
//  Barrier City
//
//  퀘스트 HUD를 사용자 머리 옆에 부드럽게 따라오게 둔다(lazy-follow).
//  자체 ARKitSession + WorldTrackingProvider로 head 포즈를 조회한다
//  (HandTrackingManager의 세션과 독립 — 한 앱에 세션 여러 개 허용).
//  head 포즈를 못 얻으면(예: 미지원) 씬 원점 앞 고정 배치로 폴백한다.
//

import ARKit
import RealityKit
import QuartzCore
import simd

@MainActor
final class QuestHUDFollower {
    private let session = ARKitSession()
    private let provider = WorldTrackingProvider()
    private var running = false
    private var placed = false   // 최초 1회는 스무딩 없이 즉시 배치

    /// world tracking 시작. 실패하면 running=false로 남아 update가 폴백 배치를 쓴다.
    func start() async {
        guard WorldTrackingProvider.isSupported else {
            print("WorldTracking 미지원 — 퀘스트 HUD 고정 배치 폴백")
            return
        }
        do {
            try await session.run([provider])
            running = true
        } catch {
            print("WorldTracking 시작 실패: \(error) — 퀘스트 HUD 고정 배치 폴백")
        }
    }

    /// 매 프레임 호출. panel을 head 옆 타깃으로 스무딩 이동 + head를 향해 yaw 빌보드.
    /// - panel: 씬 루트에 붙은 HUD 엔티티(월드 좌표계에서 움직인다).
    /// - dt: 이전 프레임과의 시간 간격(초, SceneEvents.Update.deltaTime).
    func update(panel: Entity, dt: Float) {
        let targetPos: SIMD3<Float>
        let headPos: SIMD3<Float>
        if let pose = targetPose() {
            targetPos = pose.position
            headPos = pose.head
        } else {
            targetPos = QuestTuning.fallbackPosition
            headPos = SIMD3(0, QuestTuning.fallbackPosition.y, 0)   // 원점을 향함
        }

        let current = panel.position(relativeTo: nil)
        var newPos: SIMD3<Float>
        if !placed {
            newPos = targetPos          // 최초 즉시 배치
            placed = true
        } else {
            // 데드존: head 기준 두 방향 사잇각 + 거리로 판정.
            let angle = angleBetween(current - headPos, targetPos - headPos)
            let dist = simd_distance(current, targetPos)
            if angle > QuestTuning.deadZoneAngle || dist > QuestTuning.deadZoneDistance {
                let t = 1 - exp(-QuestTuning.smoothingRate * dt)   // 지수 스무딩
                newPos = current + (targetPos - current) * t
            } else {
                newPos = current
            }
        }
        panel.setPosition(newPos, relativeTo: nil)

        // 빌보드: head를 향해 yaw만(InteractionSetup 관례: yaw = atan2(dir.x, dir.z)).
        let dir = SIMD3(headPos.x - newPos.x, 0, headPos.z - newPos.z)
        if simd_length(dir) > 1e-5 {
            panel.setOrientation(simd_quatf(angle: atan2(dir.x, dir.z), axis: [0, 1, 0]),
                                 relativeTo: nil)
        }
    }

    func stop() {
        running = false
        session.stop()
    }

    /// head 포즈로부터 (타깃 위치, head 위치)를 계산. 못 얻으면 nil.
    private func targetPose() -> (position: SIMD3<Float>, head: SIMD3<Float>)? {
        guard running,
              let anchor = provider.queryDeviceAnchor(atTimestamp: CACurrentMediaTime())
        else { return nil }
        let m = anchor.originFromAnchorTransform
        let head = SIMD3(m.columns.3.x, m.columns.3.y, m.columns.3.z)
        // head yaw만 사용(pitch/roll 무시): forward(-Z)/right(+X)를 수평면에 투영.
        let forward = normalizeSafe(-SIMD3(m.columns.2.x, 0, m.columns.2.z))
        let right = normalizeSafe(SIMD3(m.columns.0.x, 0, m.columns.0.z))
        let pos = head
            + forward * QuestTuning.forwardDistance
            + right * QuestTuning.lateralOffset
            + SIMD3(0, QuestTuning.verticalOffset, 0)
        return (pos, head)
    }
}

// MARK: - simd 헬퍼
private func angleBetween(_ a: SIMD3<Float>, _ b: SIMD3<Float>) -> Float {
    let la = simd_length(a), lb = simd_length(b)
    guard la > 1e-5, lb > 1e-5 else { return 0 }
    return acos(max(-1, min(1, simd_dot(a, b) / (la * lb))))
}
private func normalizeSafe(_ v: SIMD3<Float>) -> SIMD3<Float> {
    let l = simd_length(v)
    return l > 1e-5 ? v / l : SIMD3(0, 0, -1)
}
```

- [ ] **Step 2: QuestSetup을 follower 사용으로 교체**

파일: `Barrier City/Quest/QuestSetup.swift` 전체를 아래로 교체(고정 배치 → follower 갱신):
```swift
//
//  QuestSetup.swift
//  Barrier City
//
//  퀘스트 HUD 설치 훅. InteractionSetup.install() 끝에서 한 번 호출된다.
//  HUD attachment를 '씬 루트'(worldRoot가 아니라 content)에 붙여 맵과 분리하고,
//  QuestHUDFollower로 매 프레임 head 옆에 lazy-follow 시킨다.
//

import RealityKit
import SwiftUI

@MainActor
enum QuestSetup {
    private static var follower: QuestHUDFollower?
    private static var hudPanel: Entity?
    private static var subscription: EventSubscription?

    static func install(content: RealityViewContent,
                        attachments: RealityViewAttachments,
                        appModel: AppModel) {
        // 재진입마다 1단계로 리셋(InteractionModel 리셋 패턴과 동일).
        QuestModel.shared.reset()

        guard let panel = attachments.entity(for: "questHUD") else {
            print("⚠️ questHUD attachment 없음 — 퀘스트 HUD 비활성")
            return
        }
        content.add(panel)   // 씬 루트에 직접(HUD는 맵과 함께 움직이면 안 된다)
        hudPanel = panel

        let f = QuestHUDFollower()
        follower = f
        Task { await f.start() }

        subscription = content.subscribe(to: SceneEvents.Update.self) { event in
            guard let panel = hudPanel, let f = follower else { return }
            f.update(panel: panel, dt: Float(event.deltaTime))
        }
    }
}
```

- [ ] **Step 3: 빌드 확인**

Run:
```
xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 4: 빌드 산출물 정리**

Run:
```
git restore "Barrier City.xcodeproj/project.pbxproj"
find "Barrier City.xcodeproj" -name "*.rcuserdata" -exec rm -rf {} + 2>/dev/null; true
git status --short
```
Expected: 변경은 `Barrier City/Quest/QuestHUDFollower.swift`, `Barrier City/Quest/QuestSetup.swift` 2개만.

- [ ] **Step 5: 시뮬레이터 수동 확인**

시뮬레이터 실행 → 몰입 진입 시 HUD가 사용자 앞-왼쪽에 뜬다. 시뮬레이터에서 시야를 회전(옵션+드래그로 카메라 방향 전환)하면 HUD가 데드존을 벗어날 때 부드럽게 따라온다. (WorldTracking이 안 잡히면 원점 앞 고정 — 그래도 HUD는 보인다.)

- [ ] **Step 6: 커밋**

```bash
git add "Barrier City/Quest/QuestHUDFollower.swift" "Barrier City/Quest/QuestSetup.swift"
git commit -m "feat(quest): head lazy-follow(WorldTracking) + 데드존·스무딩·폴백"
```

---

## Task 6: 이벤트 연결 — 진행 트리거

**Files:**
- Modify: `Barrier City/Interaction/SceneSwitcher.swift` (switchToIndoor 성공 끝)
- Modify: `Barrier City/Interaction/KioskOrderView.swift` (사용하기 버튼)

**Interfaces:**
- Consumes: `QuestModel.shared.advance(on:)`(Task 2).
- Produces: 없음(말단 연결).

- [ ] **Step 1: 실내 진입 시 `.enteredIndoor` 발행**

파일: `Barrier City/Interaction/SceneSwitcher.swift` — switchToIndoor의 성공 경로 마지막(`im.kioskPanelEntity?.isEnabled = false` 다음, 함수 닫기 전)에 추가:
```swift
        im.panelEntity?.isEnabled = false
        im.kioskPanelEntity?.isEnabled = false

        // [김현기] 퀘스트: 실내(카페) 진입 완료 → 다음 단계로.
        QuestModel.shared.advance(on: .enteredIndoor)
    }
```

- [ ] **Step 2: 키오스크 "사용하기" 시 `.kioskFailed` 발행**

파일: `Barrier City/Interaction/KioskOrderView.swift` — 버튼 액션에 한 줄 추가:
```swift
                Button {
                    im.kioskTooHighShown = true
                    QuestModel.shared.advance(on: .kioskFailed)
                } label: {
```

- [ ] **Step 3: 빌드 확인**

Run:
```
xcodebuild -project "Barrier City.xcodeproj" -scheme "Barrier City" -destination 'generic/platform=visionOS Simulator' build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 4: 빌드 산출물 정리**

Run:
```
git restore "Barrier City.xcodeproj/project.pbxproj"
find "Barrier City.xcodeproj" -name "*.rcuserdata" -exec rm -rf {} + 2>/dev/null; true
git status --short
```
Expected: 변경은 `Barrier City/Interaction/SceneSwitcher.swift`, `Barrier City/Interaction/KioskOrderView.swift` 2개만.

- [ ] **Step 5: 시뮬레이터 전체 흐름 확인**

1. 진입 → HUD 1단계("카페 입구로 이동하세요").
2. 문 접근 → "예" → 실내 진입 → "완료!" 연출 후 2단계("키오스크에서 주문을 시도해 보세요")로 전환.
3. 키오스크 접근 → "키오스크 사용하기" → 장벽 안내와 함께 3단계("직원에게 도움을 요청하세요")로 전환.
4. 체험 종료 후 재진입 → 1단계로 리셋(회귀 확인).
5. 기존 문·키오스크 인터랙션 정상(회귀 확인).

- [ ] **Step 6: 커밋**

```bash
git add "Barrier City/Interaction/SceneSwitcher.swift" "Barrier City/Interaction/KioskOrderView.swift"
git commit -m "feat(quest): 실내 진입·키오스크 실패 이벤트를 퀘스트 진행에 연결"
```

---

## Self-Review (작성자 체크 완료)

**1. 스펙 커버리지:**
- 목적(가이드 HUD, 알림+상시) → Task 3(완료 연출 + 상시 목표 카드) ✓
- Quest/ 4개 파일 → QuestProgression(T1), QuestModel(T2), QuestHUDView(T3), QuestHUDFollower(T5), QuestSetup(T4/5) ✓ (스펙 4파일 + 순수 로직 분리 1파일 = 5파일; 진행 판정을 단독 테스트하려고 QuestProgression을 분리 — 스펙의 "순수 로직으로 작성" 요구에 부합)
- 이윤서 1줄 수정 → Task 4 Step 3 ✓
- 3단계 문구(실기기 기준) → QuestModel steps, 스펙 표 그대로 ✓
- lazy-follow(자체 세션, 데드존, 지수 스무딩, 빌보드) → Task 5 ✓
- 폴백 고정 배치 → QuestHUDFollower.update 폴백 + QuestTuning.fallbackPosition ✓
- 씬 루트 부모(맵과 분리) → QuestSetup `content.add(panel)` ✓
- 재진입 리셋 → QuestSetup `QuestModel.shared.reset()` ✓
- `.npcHelpDone` 정의만 → QuestEvent에 존재, 발행처 없음 ✓
- 빌드 후 pbxproj/rcuserdata 정리 → 모든 빌드 태스크에 정리 스텝 ✓
- 튜닝 상수 단일 진실원 → QuestTuning ✓

**2. 플레이스홀더 스캔:** 모든 코드 스텝에 완성 코드 포함, TBD/TODO 없음. ✓

**3. 타입 일관성:** `nextIndex(currentIndex:stepCount:eventMatchesCurrent:)`(T1 정의 = T2 호출), `advance(on:)`/`reset()`/`currentStep`/`justCompletedStep`(T2 정의 = T3/T4/T6 사용), `QuestHUDFollower.update(panel:dt:)`/`start()`(T5 정의 = QuestSetup 사용), `install(content:attachments:appModel:)`(T4 정의 = InteractionSetup 호출) — 시그니처 일치 확인. ✓
